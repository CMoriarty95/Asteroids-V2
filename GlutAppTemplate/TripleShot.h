#pragma once
#include "powerUp.h"
#include "App.h"
#include "Timer.h"
#include "Asteroid.h"
#include "Missile.h"


class TripleShot : public powerUp {
public:
	/*
	Timer shotTimer;
	bool tripShot = false;

	TripleShot()
	{
		r = 1;
		g = 0;
		b = 0;
	}

	bool tripleStatus() {
		return tripShot;
		};

		//	shotTimer.setTimer();

	bool shotStatus() {
		if (shotTimer.compareTime() > 30) {
			tripShot = false;
		}

	}
	*/
};